ktor {
  application {
    modules = [ com.kotlinadmin.ApplicationKt.module ]
  }
  deployment {
    port = 8002
    port = ${?APP_PORT}
  }
}

app {
  name = "KotlinAdmin"
  name = ${?APP_NAME}
  mode = "full"
  mode = ${?APP_MODE}
  jwtSecret = "change-me-in-production-min-32-chars"
  jwtSecret = ${?JWT_SECRET}
  jwtExpiresIn = "1h"
  jwtExpiresIn = ${?JWT_EXPIRES_IN}
  sessionSecret = "change-me-session-secret-min-32xx"
  sessionSecret = ${?SESSION_SECRET}
  sessionTtlHours = 6
  sessionTtlHours = ${?SESSION_TTL_HOURS}
  sessionDriver = "database"
  sessionDriver = ${?SESSION_DRIVER}
  bcryptRounds = 10
  bcryptRounds = ${?BCRYPT_ROUNDS}
  otpExpiryMinutes = 10
  otpExpiryMinutes = ${?OTP_EXPIRY_MINUTES}
  defaultPageSize = 10
  defaultPageSize = ${?DEFAULT_PAGE_SIZE}
}

storage {
  driver = "local"
  driver = ${?STORAGE_DRIVER}
  basePath = "uploads"
  basePath = ${?STORAGE_BASE_PATH}
  accessKeyId = ""
  accessKeyId = ${?STORAGE_ACCESS_KEY_ID}
  secretAccessKey = ""
  secretAccessKey = ${?STORAGE_SECRET_ACCESS_KEY}
  endpoint = ""
  endpoint = ${?STORAGE_ENDPOINT}
  bucket = ""
  bucket = ${?STORAGE_BUCKET}
  region = ""
  region = ${?STORAGE_REGION}
  ssl = true
  ssl = ${?STORAGE_SSL}
}

mail {
  host = ""
  host = ${?MAIL_HOST}
  port = 587
  port = ${?MAIL_PORT}
  secure = false
  secure = ${?MAIL_SECURE}
  username = ""
  username = ${?MAIL_USERNAME}
  password = ""
  password = ${?MAIL_PASSWORD}
  fromName = "KotlinAdmin"
  fromName = ${?MAIL_FROM_NAME}
  fromAddress = ""
  fromAddress = ${?MAIL_FROM_ADDRESS}
}

database {
  # DB_TYPE = sqlite (default) | mysql | postgres. URL & driver JDBC diturunkan
  # dari type + host/port/name di AppConfig.resolveDb.
  type = "sqlite"
  type = ${?DB_TYPE}
  host = "localhost"
  host = ${?DB_HOST}
  port = ${?DB_PORT}
  name = "kotlinadmin"
  name = ${?DB_DATABASE}
  name = ${?DB_NAME}

  # Override penuh — isi hanya bila butuh parameter JDBC khusus (sslmode, dsb).
  # Dibiarkan kosong agar DB_TYPE di atas yang menentukan.
  url = ""
  url = ${?DB_URL}
  driver = ""
  driver = ${?DB_DRIVER}

  user = ""
  user = ${?DB_USERNAME}
  user = ${?DB_USER}
  password = ""
  password = ${?DB_PASSWORD}

  # Batas pool HikariCP; jatah koneksi DB managed dibagi ke semua replika.
  maxPool = 3
  maxPool = ${?DB_MAX_POOL}
}

redis {
  url = "redis://127.0.0.1:6379"
  url = ${?REDIS_URL}
  host = "localhost"
  host = ${?REDIS_HOST}
  port = 6379
  port = ${?REDIS_PORT}
  password = ${?REDIS_PASSWORD}
}
